services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    volumes:
      - uploads_data:/app/public/uploads
    restart: unless-stopped

volumes:
  uploads_data:
